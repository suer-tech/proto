# 📦 Deployment Files Overview

Этот документ описывает файлы, связанные с развертыванием приложения Protocol Maker на VPS.

## 📚 Документация

### 1. **DEPLOYMENT_GUIDE.md** (Полное руководство)
Полный пошаговый гайд по развертыванию с подробными инструкциями для каждого шага.

**Включает:**
- Установку всех зависимостей
- Настройку Python окружения
- Создание systemd сервисов
- Настройку Nginx reverse proxy
- Настройку SSL сертификатов
- Решение проблем
- Оптимизацию производительности

**Используйте:** Если вы развертываете приложение впервые или нужны подробные инструкции.

---

### 2. **QUICK_DEPLOY.md** (Быстрый старт)
Краткое руководство для опытных пользователей.

**Включает:**
- Быстрая установка за 5 минут
- Основные команды
- Сценарий обновления
- Устранение неполадок

**Используйте:** Если вы уже знакомы с развертыванием и нужны только команды.

---

## 🛠️ Скрипты и конфигурация

### 3. **deploy.sh** (Автоматический скрипт)
Автоматизирует процесс развертывания.

**Что делает:**
- Проверяет все зависимости
- Устанавливает Python зависимости
- Создаёт .env файл (если не существует)
- Собирает фронтенд
- Создаёт systemd сервисы
- Запускает все сервисы

**Использование:**
```bash
chmod +x deploy.sh
./deploy.sh
```

---

### 4. **nginx-protocol-maker.conf** (Nginx конфигурация)
Готовая конфигурация для Nginx reverse proxy.

**Особенности:**
- Настроенные прокси для backend и frontend
- Увеличенные лимиты для загрузки файлов
- Поддержка CORS
- Подготовка для SSL

**Использование:**
```bash
sudo cp nginx-protocol-maker.conf /etc/nginx/sites-available/protocol-maker
sudo ln -s /etc/nginx/sites-available/protocol-maker /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🔄 Изменения в коде

### 5. **backend/main.py** (Обновлено)
Добавлена поддержка динамической настройки CORS через переменные окружения.

**Изменения:**
- CORS origins теперь берутся из переменной окружения `CORS_ORIGINS`
- Поддержка нескольких доменов через запятую
- Fallback на localhost для разработки

---

## 📋 Рекомендуемый процесс развертывания

### Для новичков:
1. Прочитайте `QUICK_DEPLOY.md`
2. Выполните `./deploy.sh`
3. Настройте `.env` файл
4. Перезапустите сервисы
5. Настройте Nginx (см. `DEPLOYMENT_GUIDE.md`)

### Для опытных:
1. Выполните `./deploy.sh`
2. Отредактируйте `.env`
3. Скопируйте `nginx-protocol-maker.conf`
4. Настройте SSL
5. Готово!

---

## 🔗 Связанные файлы в проекте

- `backend/main.py` - FastAPI бэкенд
- `server/index.ts` - Express сервер для фронтенда
- `package.json` - Зависимости фронтенда
- `backend/requirements.txt` - Python зависимости
- `vite.config.ts` - Конфигурация сборки фронтенда

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи:
   ```bash
   sudo journalctl -u protocol-maker-backend -f
   sudo journalctl -u protocol-maker-frontend -f
   ```

2. Проверьте статус сервисов:
   ```bash
   sudo systemctl status protocol-maker-backend
   sudo systemctl status protocol-maker-frontend
   ```

3. Проверьте порты:
   ```bash
   sudo netstat -tlnp | grep -E '3000|3001'
   ```

4. Проверьте конфигурацию Nginx:
   ```bash
   sudo nginx -t
   ```

---

## ✅ Чек-лист развертывания

- [ ] VPS с Ubuntu/Debian
- [ ] Python 3.10+ установлен
- [ ] Node.js 18+ и pnpm установлены
- [ ] Nginx установлен (опционально)
- [ ] Проект склонирован на сервер
- [ ] .env файл настроен с API ключами
- [ ] Зависимости установлены
- [ ] Фронтенд собран
- [ ] Systemd сервисы созданы и запущены
- [ ] Nginx настроен (опционально)
- [ ] SSL сертификат установлен (опционально)
- [ ] Файрвол настроен
- [ ] Приложение работает!

---

## 🎉 Готово!

После завершения развертывания ваше приложение будет доступно по адресу:
- Backend API: `http://your-server:3001`
- Frontend: `http://your-server:3000`
- Через Nginx: `https://your-domain.com` (после настройки SSL)

