# Быстрый старт: Деплой на сервер

## 🚀 Первый деплой (5 минут)

### 1. Запустите скрипт деплоя

```bash
./deploy-to-server.sh
```

Скрипт автоматически:
- ✅ Подключится к серверу через SSH
- ✅ Установит все зависимости
- ✅ Развернет приложение в `/opt/apps/protocol-maker`
- ✅ Настроит systemd сервисы
- ✅ Настроит Nginx

### 2. Настройте API ключи

После деплоя обновите `.env` файл на сервере:

```bash
./manage-app.sh ssh
# На сервере:
nano /opt/apps/protocol-maker/.env
```

Обязательно обновите:
- `OPENAI_API_KEY` - ваш OpenAI API ключ
- `HUGGINGFACE_HUB_TOKEN` - ваш Hugging Face токен

### 3. Перезапустите сервисы

```bash
./manage-app.sh restart
```

## 📋 Управление приложением

```bash
# Статус сервисов
./manage-app.sh status

# Запуск
./manage-app.sh start

# Остановка
./manage-app.sh stop

# Перезапуск
./manage-app.sh restart

# Логи
./manage-app.sh logs
```

## 🌐 Доступ к приложению

После деплоя приложение доступно по адресу:
- **http://user176.98.234.178** (через Nginx)
- **http://user176.98.234.178:3000** (фронтенд)
- **http://user176.98.234.178:3001** (бэкенд API)

## 📚 Подробная документация

- [DEPLOY_ISOLATED.md](./DEPLOY_ISOLATED.md) - Полное руководство по изолированному деплою
- [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - Общее руководство по развертыванию

## ⚠️ Важно

1. **SSH ключ**: Файл `user` содержит приватный ключ - не коммитьте его в git!
2. **API ключи**: Обязательно обновите API ключи после первого деплоя
3. **Порты**: Приложение использует порты 3000 и 3001 - убедитесь, что они свободны

## 🔧 Устранение проблем

Если что-то пошло не так:

```bash
# Проверьте статус
./manage-app.sh status

# Проверьте логи
./manage-app.sh logs-backend
./manage-app.sh logs-frontend

# Подключитесь к серверу для диагностики
./manage-app.sh ssh
```

